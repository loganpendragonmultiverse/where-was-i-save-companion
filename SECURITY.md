# Security

Report vulnerabilities through GitHub private vulnerability reporting. Card files may contain spoilers, personal notes, or identifiable play history. The tool stays local, never contacts a game service, omits private notes by default, and refuses to replace reports. Review files before sharing them.
